const tab = [1, 2, 3, 4, 5]

// console.log(tab[0])
// pętla for do wyświetlania tablicy

for (let i=0 ; i<tab.length+4 ; i++){
    tab[i] *=2
    console.log(tab[i])
    break;
}

// nie działa tak jak na kursie???